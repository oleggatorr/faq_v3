# Данные для подключения к MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=faq_db_v2
DB_USER=root
DB_PASSWORD=2xs_khHv0352

# Строка подключения (формируется автоматически или прописывается явно)
DATABASE_URL=mysql+mysqlconnector://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}